git clone --mirror https://github.com/bbarclay/amazingmail.git temp-repo.git && cd temp-repo.git && pip install git-filter-repo && git filter-repo --path .aider.input.history --path .aider.chat.history.md --invert-paths && git reflog expire --expire=now --all && git gc --prune=now --aggressive && echo -e "\n.aider.chat.history.md\n.aider.input.history" >> .gitignore && git add .gitignore && git commit -m "Add .aider.chat.history.md and .aider.input.history to .gitignore" && git remote remove origin && git remote add origin https://github.com/bbarclay/amazingmail.git && git push origin --force --all && git push origin --force --tags && cd .. && rm -rf temp-repo.git

git checkout main && git merge --no-ff --no-edit $(git branch --no-color | sed '/^\*/d' | tr -d ' ') && git checkout -b Brandon-edit-settings10